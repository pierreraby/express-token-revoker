{
  "name": "express-token-revoker-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Monorepo for express-token-revoker — Bloom-filter token revocation for Express: standalone core (published) plus distributed server/node (alpha, private).",
  "author": "Pierre Raby",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "test:watch": "pnpm --filter express-token-revoker test:watch",
    "test:coverage": "pnpm --filter express-token-revoker test:coverage",
    "start": "pnpm --filter express-token-revoker start",
    "dev": "pnpm --filter express-token-revoker dev",
    "client": "pnpm --filter express-token-revoker client",
    "bench": "pnpm --filter express-token-revoker bench",
    "bench:throughput": "pnpm --filter express-token-revoker bench:throughput",
    "bench:jwt": "pnpm --filter express-token-revoker bench:jwt",
    "bench:fpr": "pnpm --filter express-token-revoker bench:fpr",
    "bench:memory": "pnpm --filter express-token-revoker bench:memory"
  }
}
